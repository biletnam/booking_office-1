<%= form_for(railway_station) do |f| %>
  
  

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit "#{ railway_station.new_record? ? 'Create' : 'Save edits' }" %>
  </div>
<% end %>
