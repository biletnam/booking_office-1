<tr>
  <td><%= train.number %></td>
  <td><%= link_to t('common.show'), [:admin, train], class: 'btn btn-default btn-xs' %></td>
  <td><%= link_to t('common.edit'), edit_admin_train_path(train), class: 'btn btn-default btn-xs' %></td>
  <td><%= link_to t('common.delete'), [:admin, train], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default btn-xs' %></td>
</tr>