<p>
  <strong>Route stations:</strong>
</p>
<p>
  <% if @route.railway_stations.empty? %>
    Empty list.
  <% else %>
    <% @route.railway_stations.each do |station| %>
      <ul>
        <li><%= station.title %></li>
      </ul>
    <% end %>
  <% end %>
</p>